{
  "additionalProperties": false,
  "properties": {
    "id": {
      "maxLength": 120,
      "minLength": 3,
      "pattern": "^[a-z0-9]+(?:[.-][a-z0-9]+)*$",
      "title": "Id",
      "type": "string"
    },
    "slug": {
      "maxLength": 80,
      "minLength": 2,
      "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
      "title": "Slug",
      "type": "string"
    },
    "title": {
      "maxLength": 140,
      "minLength": 4,
      "title": "Title",
      "type": "string"
    },
    "summary": {
      "maxLength": 400,
      "minLength": 20,
      "title": "Summary",
      "type": "string"
    },
    "order": {
      "minimum": 1,
      "title": "Order",
      "type": "integer"
    }
  },
  "required": [
    "id",
    "slug",
    "title",
    "summary",
    "order"
  ],
  "title": "Module",
  "type": "object"
}
